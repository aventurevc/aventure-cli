# aventure-cli

The public aVenture CLI.

```bash
npm install -g @aventurevc/aventure-cli
aventure status
aventure help
```

The command catalog is generated from the public aVenture OpenAPI spec and must not be hand-edited.
